ALGORITHM Sorting
VAR
    A:TAB: ARRAY_OF INTEGER[n];
BEGIN
    FOR i FROM 0 TO n-1 STEP   DO
         j = i
         WHILE (j > 0 and A[j-1] > A[j]) DO
             swap(A[j], A[j-1])
           j = j - 1
         END_WHILE
    END_FOR
END